# TODO

- [x] Add timeout to `waitForFresh` to prevent silent infinite polling
- [ ] Add `useSettingsSchema` to expose `toolbar` and `toolbarShortcut` in plugin settings UI
- [x] Guard `mountToolbar` against overlapping calls on rapid page navigation
- [ ] Improve toolbar positioning to appear near selection end for multi-line selections
